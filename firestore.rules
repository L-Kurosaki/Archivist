rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // All access is handled via the Express backend (firebase-admin)
      allow read, write: if false;
    }
  }
}
